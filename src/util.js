const Enum = require('enum');

const util = {
  // closeCode: new Enum({
  //   NORMAL_CLOSE: 1000,
  //   INVALID_MESSAGE_DATA: 1003,
  //   ABSENT_FIELD: 4000,
  //   INVALID_MESSAGE_TYPE: 4001,
  // }),
};

module.exports = util;
